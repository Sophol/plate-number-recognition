[pytest]
asyncio_mode = auto
testpaths = tests
# Camera-worker loops reconnect forever by design; a hung test should fail loudly.
timeout = 60
markers =
    slow: renders a video and runs it through the full pipeline (seconds, not ms)
